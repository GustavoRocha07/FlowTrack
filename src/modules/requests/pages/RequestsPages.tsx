import { Container, Typography } from '@mui/material';

export function RequestsPage() {
  return (
    <Container>
      <Typography variant="h4">Solicitações</Typography>
      <Typography>Listagem inicial de solicitações</Typography>
    </Container>
  );
}